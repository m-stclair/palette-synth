{
  "type": "module",
  "scripts": {
    "check": "node tools/check-js.js",
    "test": "node --test tests/*.test.js",
    "test:all": "npm run check && npm test && npm run e2e:if-available",
    "e2e": "playwright test",
    "e2e:if-available": "node tools/run-e2e-if-browser.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0"
  }
}
